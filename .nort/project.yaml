#
# mo release configuration 
#
qualification:
  os.command.to.get.installed.version: mo version

build:

publish:
  local.artifact.repository.root: ${M2}

install:
  installation.directory: ${RUNTIME_DIR}
